#include <stdio.h>
#include "ctest.h"
#define max matches 100
#include 100-matches.h

int main
{
	int player = 1;
	int ost;
	int take;
	ost = max matches
	if player = 1
	{
		printf ("You move. In the table", ost, "matches");
		printf ("How many matches You take?");
		scanf ("%d", &take);
	}
	

return 0;
}
